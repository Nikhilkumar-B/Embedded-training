#include <stdio.h>
#include <stdlib.h>

int main()
{
    int sum,size,ctr=0,i;
    printf("enter the size \n");
    scanf("%d",&size);
    int arr[size];
    printf("enter the elements \n");
    scanf("%d",&arr[i]);
    for(i=0;i<size;i++)
    {

        if(arr[i]==arr[i])
        {

            printf("%d",arr[i]);
        }
    }
    return 0;
}
